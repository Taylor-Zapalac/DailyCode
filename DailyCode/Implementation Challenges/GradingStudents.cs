using System;
using System.Collections.Generic;
using System.Text;

namespace DailyCode.Implementation_Challenges
{
    class GradingStudents
    {
        //Every student receives a grade in the inclusive range from 0 to 100.
        //Any grade less than 40 is a failing grade.
        // If the difference between the grade and the next multiple of 5 is less than 3, round grade up to the next multiple of 5.
        // If the value of  is less than 38, no rounding occurs as the result will still be a failing grade.

        //Input n = number of students, int[] grade, each students grade

        public int[] roundGrade(int students, int[] grades)
        {


            return null;
        }
    }
}
